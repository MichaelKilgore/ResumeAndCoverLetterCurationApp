Based on the following job description:

{job_description}

First I need you to identify the hard and soft skills in the job description, similar to how job scan works. Don't rephrase the keywords in any way though. I want my final resume to include as many keywords as possible. This means for example if Python3 is in the job description and I have Python in my resume that is not a match. The only thing you can disregard is capitalization for matches.

Here is the necessary data:

{data}

1. For each of the job titles you will pull the 4 best bullet points based on these criteria:

  1. The number of key words that the bullet point covers. Remember its important that the key word is an exact match so SQL is not the same as postgreSQL for example.
  2. Additionally the variety of bullet points, across all the data we pull across job / project bullet points and skills we want to cover as many key words as many times as possible.
  3. Note that some of these bullet points are reworded versions of other bullet points, make sure not to include duplicates.
  4. Finally order the bullets by a combination of how good a bullet point is and how many key words it has. A bullet point is good if it seems complex and makes use of data points such as improved forecasting by 10%.


2. return all the skills given but remove duplicates. Like for example Python and Python3 are the same, pick which one to display based on whether or not it is a valid key word, if they both either are or are not key words then just pick one. Also try and keep related languages together in list response, like for example CSS and HTML are associated together so put them close together.

3. You also need to pick one project and the top 4 best bullet points based on the same criteria as the criteria for the jobs. But remember we want to cover as many key words as possible across jobs, skills, and project.

4. Lastly also return the keywords I am currently missing as well, but disregard the ignored keywords. For ignored keywords they don't have to be exact matches. So if I say ignore JDK then you can also ignore JDK17 or JDK21.
